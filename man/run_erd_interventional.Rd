% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/erd-interventional.R
\name{run_erd_interventional}
\alias{run_erd_interventional}
\title{Interventional Exposure-Response Decomposition (ERD)}
\usage{
run_erd_interventional(
  scee,
  exposure,
  celltype,
  celltype_col,
  sample_col = "donor_id",
  covariates = NULL,
  target_genes = NULL,
  contrast = c(1, 0),
  n_mc = 200L,
  n_boot = 2000L,
  interaction = FALSE,
  min_cells = 10L,
  min_donors = 30L,
  min_group_donors = 10L,
  min_boot_valid = 0.9,
  adjust = c("none", "within_call"),
  seed = 20260703L,
  BPPARAM = NULL
)
}
\arguments{
\item{scee}{A \linkS4class{SingleCellExposomeExperiment}.}

\item{exposure}{Character; column of \code{exposureData(scee)} (donor-level).}

\item{celltype}{Character; the target cell type whose expression is the
outcome (pseudobulk within this type).}

\item{celltype_col}{Character; \code{colData} column with cell-type labels.}

\item{sample_col}{Character; donor id column (default "donor_id").}

\item{covariates}{Character vector; donor covariate columns in
\code{exposureData} (numeric).}

\item{target_genes}{Character; genes to test (default: all).}

\item{contrast}{Numeric length-2, \code{c(a, a*)} exposure levels
(default \code{c(1, 0)}; for continuous exposure use two chosen values).}

\item{n_mc}{Monte-Carlo mediator draws per donor (default 200).}

\item{n_boot}{Donor bootstrap replicates for CIs (default 2000).}

\item{interaction}{Logical; include A x M in the outcome model. The default
is \code{FALSE}; interaction models require substantially more donors.}

\item{min_cells}{Integer; minimum target-cell count per donor.}

\item{min_donors}{Integer; minimum eligible donors.}

\item{min_group_donors}{Integer; for a binary exposure, minimum eligible
donors in each group.}

\item{min_boot_valid}{Numeric in (0, 1]; minimum fraction of finite bootstrap
estimates required for an interval and p-value.}

\item{adjust}{Multiplicity adjustment scope. The default \code{"none"}
prevents a per-cell-type call from being mistaken for study-wide FDR.
Use \code{"within_call"} only when this call is the complete hypothesis
family; otherwise combine calls and adjust globally downstream.}

\item{seed}{Integer RNG seed.}

\item{BPPARAM}{A \code{BiocParallel} param; use \code{SnowParam} on Windows.}
}
\value{
A data.frame, one row per gene: IDE_RR, IIE_RR, OE_RR with bootstrap
  95\% CIs, proportion mediated, per-effect mediational E-values (point and
  CI-limit), the closed-form cross-check, finite-resampling p-values, and
  optional within-call BH values.
}
\description{
Decomposes a donor-level exposure's effect on within-cell-type pseudobulk
expression into an interventional direct effect (within-composition) and an
interventional indirect effect (cell-composition-driven), on the rate-ratio
scale, by counterfactual g-computation with a compositional ILR mediator.
}
\details{
This is a developmental function and is not exported. Its outcome is a
within-cell-type rate, which cannot identify the contribution of cell-type
abundance to a tissue mixture, so its direct and indirect effects and
E-values must not be read causally or used for confirmatory claims. It is
kept, and tested, so the estimand it was built around can be revisited; call
it as \code{exposomeSC:::run_erd_interventional()}.
}
\keyword{internal}
